# Security Policy

This project is static-only and designed with a minimal attack surface.

Recommended deployment headers are included in `public/_headers`.

Do not add third-party scripts unless necessary. Any future dynamic search or rendering must avoid unsafe DOM APIs and validate client-side input.
